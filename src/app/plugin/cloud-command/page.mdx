import {Button} from "../../../components/Button";
export const metadata = {
    title: "Command Plugin",
    description: "Manage your server network with powerful in-game proxy commands"
}

# Command Plugin

The Cloud Command Plugin empowers your network with in-game proxy commands, enabling efficient cloud management directly from within the game. Easily start, stop, and manage servers using commands. {{ className: 'lead' }}

<Button href="https://github.com/theSimpleCloud/command-plugin/releases" arrow="right">
    <>Download</>
</Button>

## Quick Setup

1. Download the plugin for your proxy software
2. Place it in your proxy template's plugin folder
3. Start your proxy server

## Start Command
Starts a server from a specified group

```
cloud start <group>
```

## Stop Command
Stops a specific server from a specified group.

```
cloud stop <group> <id>
```

## Get Servers Command
Retrieves one or multiple servers from a specified group

```
cloud get servers [group] [id]
```

## Get Groups Command
Retrieves one or all groups

```
cloud get groups [group]
```

## What's Next?

- [Server API Documentation](/api#server)
- [Proxy Configuration](/proxy)
- [Plugin Development](/plugins)